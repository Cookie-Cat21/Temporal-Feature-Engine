FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY ring_hunter.py .

ENV MEMGRAPH_URI=bolt://memgraph:7687
ENV CHECK_INTERVAL=30

CMD ["python", "ring_hunter.py"]
